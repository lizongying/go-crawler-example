mongo:
  example:
    uri: mongodb://127.0.0.1:27017
    database: crawler
log:
  filename: /tmp/spider_{name}.log
  long_file: true
  level: info
proxy:
  example:
request:
  concurrency: 1
  delay: 1